<h1>Campaigns</h1>
<h4>View and Manage your Campaigns</h4>
<div class="span-16 last redhr"></div>

<div class="span-16 last">
  <% @campaigns.each do |campaign| %>
    <%= render :partial => "shared/show_campaign", :object => campaign %>
    <%= render :partial => "shared/campaign_performance", :locals => {:campaign => campaign, :campaignPerformance => @campaignPerformance} %>
  <% end %>
</div>

<br />

<%= link_to 'New Campaign', new_campaign_path %>

